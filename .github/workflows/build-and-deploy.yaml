name: Build & Deploy
on:
  push:
  workflow_dispatch:

jobs:
  vite-mikrofrontend:
    uses: navikt/teamesyfo-github-actions-workflows/.github/workflows/vite-mikrofrontend.yaml@remove-deprecated-4
    permissions:
      contents: read
      id-token: write
      packages: write
    secrets: inherit
    with:
      app: dialogmote-mikrofrontend
      id: syfo-dialog
